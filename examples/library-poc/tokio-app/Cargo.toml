[package]
name = "tokio-app"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
session-lib = { path = "../session-lib", features = ["tokio"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "time"] }
